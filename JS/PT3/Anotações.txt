1. Math

Math -> Matemática

- Pow(2,2) / potência / 2² = 4  == const result = Math.pow(5,3)
- sqrt (25,2) /raiz quadrada / √25 == const result == Math.sqrt(25,2)
- PI  == const result = Math.PI
- Ceil / Teto -> arredodar para cima == const result = Math.ceil(3.14)
- floor / chão -> arredodar para baixo == const result = Math.floor(3.14)
-randon () / número aleatório entre 0 e 1 == const result = Math.randon()


3. Operadores Aritméticos

+ -> Adição
- -> Subtração 
/ -> Divisão 
* -> Mutiplicação
% -> Restro
++ => Incremento 
-- -> Descremento 
** -> Expotencial

4. Operadores de Atribuição

= -> Atribuição 
{para economizar tempo e codigo não precisando dijitar o codigo inteiro usamos atalhos como codigos ou seja codigo redusidos.

ex: ao enves de 
lest result = 20
resulte =resulte + 70 
console.log(result)
90
usamos o 
lest result = 20
result += 70 
console.log(result)
90 
}
.+= -> Adição
.-= -> Subtração 
./= -> Divisão 
.*= -> Mutiplicação
.%= -> Restro

5. Operadores de comparação

== -> Igual // Conpara o Valor, mais não o Tipo
=== -> TOTALMENTE Igual // Conpara o VALOR e o TIPO

!= -> Diferente // Ele compara o Valor, mais não o Tipo
!== -> TOTALMENTE Diferente // compara o VALOR e o TIPO

6. Operadores de comparação pt.2

> -> Maior
< -> Menor
>= -> Maior ou Igual
<= -> Menor ou Igual


8. Operadores type of e delete

typeof -> Serve para ver o tipo do dado
delete -> server para apagar parametros do meus dados

9. Else if

IF -> (SE)
Else -> (Se NÂO)
Else if -> outras opções do que o IF/Else 

10. Operador ternário (Modo de economizar codigo) {Criado para pequenos retornos não conseguimos usar para codigos complexos}

? = IF(SE)
: = Else(Se não)


11. Switch case (Controlador de Fluxo)

precisamos colocar o valor exato ao contrario do else e if que podemos usar valores variados 


12. SetTimeout & setInterval

SetTimeout -> Execulta um vez
setInterval -> Execulta infinitas vezes